diff --git a/comfy_api_nodes/canary.py b/comfy_api_nodes/canary.py
--- a/comfy_api_nodes/canary.py
+++ b/comfy_api_nodes/canary.py
@@ -1,11 +1,7 @@
-import av
-
-ver = av.__version__.split(".")
-if int(ver[0]) < 14:
-    raise Exception("INSTALL NEW VERSION OF PYAV TO USE API NODES.")
-
-if int(ver[0]) == 14 and int(ver[1]) < 2:
-    raise Exception("INSTALL NEW VERSION OF PYAV TO USE API NODES.")
+# PyAV version check disabled for Nix builds.
+# The comfy_api_nodes require external API credentials and are not core functionality.
+# Users needing these nodes can set COMFY_ENABLE_API_NODES=true and provide their own deps.
+pass

 NODE_CLASS_MAPPINGS = {}
